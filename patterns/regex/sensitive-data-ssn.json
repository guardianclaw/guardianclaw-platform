{
  "$schema": "../schema.json",
  "name": "sensitive-data-ssn",
  "version": "2.0.0",
  "kind": "regex",
  "gate": "avoidance",
  "flags": "i",
  "description": "Avoidance gate — US Social Security Number formats, with and without context. Canonical from the TypeScript core. Covered by parity tests.",
  "items": [
    "\\b\\d{3}-\\d{2}-\\d{4}\\b",
    "\\bssn\\s*(is|:)\\s*\\d{3}-\\d{2}-\\d{4}"
  ]
}
