{
  "$schema": "../schema.json",
  "name": "sensitive-data-passwords",
  "version": "2.0.0",
  "kind": "regex",
  "gate": "avoidance",
  "flags": "i",
  "description": "Avoidance gate — generic credential assignments: password=..., secret=..., token=..., my password is.... Quote characters expressed via hex escapes (\\x22 \\x27) so the registry source is safe under Python r-string emit and JS RegExp literal alike. Canonical from the TypeScript core. Covered by parity tests.",
  "items": [
    "(password|passwd|pwd)\\s*[=:]\\s*[\\x22\\x27]?[^\\s\\x27\\x22]{4,}",
    "(secret[_-]?key|secret|token)\\s*[=:]\\s*[\\x22\\x27]?[^\\s\\x27\\x22]{6,}",
    "my\\s+password\\s+is[:\\s]+[^\\s]{4,}",
    "\\b(api[_-]?key\\s*[=:]\\s*[\\x22\\x27]?[a-zA-Z0-9_-]{8,})"
  ]
}
